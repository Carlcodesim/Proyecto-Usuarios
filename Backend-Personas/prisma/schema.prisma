datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Persona {
  id        String   @id @default(uuid())
  nombre    String
  edad      Int
  creadoEn  DateTime @default(now())
}